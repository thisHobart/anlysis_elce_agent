# 真实来源新闻抽取基准

| 字段 | 内容 |
|---|---|
| 核对日期 | 2026-08-31 |
| 语料版本 | `1.0.0` |
| 抽取器 | `obvious-news-rule-baseline@1.1.0` |
| 总体判定 | **Needs revision / not qualified** |
| 语料 | 10 个真实来源短摘录，4 种语言标记、9 个市场标签、10 个独立 URL |
| 用途 | 新闻抽取准确率与安全弃权资格测试；不用于事件—价格效应估计 |
| 全量回归 | Ruff 通过；394 passed（131.28s） |

## 1. 为什么没有删除旧合成集

旧 `tests/fixtures/news_price/` 仍负责可控的统计回归：去重、更正、`as_of`、时间泄漏、注入效应和证据哈希。
真实新闻没有配套反事实价格，若直接替换旧集，将无法判断事件—价格方法是否恢复了已知答案。

新增 `tests/fixtures/news_realistic/` 取代旧集作为**抽取准确率证据**。测试时不访问网页，避免来源页面修改导致 CI
漂移；每条记录只冻结标题和短摘录或轻度压缩摘录，并保留原始 URL、来源层级、发布时间精度、抓取时点与内容哈希，
不保存整篇报道。

## 2. 来源与覆盖

语料优先使用监管机构、政府、市场运营商和系统运营商的一手发布，覆盖：

- 江苏机组跳闸月报；
- 广东核电站输电线路故障与重新并网；
- 全国高温负荷纪录；
- 华东新能源出力纪录；
- 英国核电长期退役；
- AEMO 双机跳闸、870 MW 损失与事件时刻；
- NESO 低风、燃气可用性、高需求、联络线与网络约束并存；
- ERCOT 需求纪录及结算前可修订语义；
- 广西机组恢复并网；
- 电力企业社区服务负对照。

人工金标准位于
[`tests/fixtures/news_realistic/gold_manifest.yaml`](../tests/fixtures/news_realistic/gold_manifest.yaml)，
抽取器输入只有
[`source_news.jsonl`](../tests/fixtures/news_realistic/source_news.jsonl)，不会读取答案文件。

## 3. 数据质量

| 检查 | 结果 |
|---|---:|
| URL 唯一且可追溯 | 10 / 10 |
| 来源层级完整 | 10 / 10 |
| 抓取时点完整 | 10 / 10 |
| 中文记录 | 6 |
| 英文记录 | 4 |
| 只有发布日期、没有发布时刻 | 8 / 10 |
| 模板标记 `TEST_*` | 0 |
| 人工补写 `事件开始：ISO时间` | 0 |

发布日期只有“日”的记录将本地 `00:00` 作为**存储占位**，并用 `published_time_precision=day` 留痕；该占位
不得被解释成新闻实际在午夜可用。真实外部 API 接入时必须提供真实取得时间，或把整日发布日期视为低精度元数据。

## 4. 当前抽取结果

资格阈值与结果：

| 指标 | 阈值 | 实际 | 判定 |
|---|---:|---:|---|
| 可按当前契约抽取的事件召回率 | ≥ 80% | 0 / 2 = 0% | 失败 |
| 应隔离新闻召回率 | ≥ 80% | 0 / 7 = 0% | 失败 |
| 无关新闻正确率 | ≥ 95% | 1 / 1 = 100% | 通过 |
| `unknown` 比率 | ≤ 20% | 10 / 10 = 100% | 失败 |
| 逐案完整准确率 | — | 1 / 10 = 10% | 不合格 |
| 真实事件证据完整性 | 100% | 无事件可评估 | 未通过警告 |

逐案结果：

| ID | 金标准 | 当前结果 | 主要困难 |
|---|---|---|---|
| R01 | 隔离：发电停运但只有日期 | `unknown` | “机组跳闸”不命中“突发/非计划停运”模板 |
| R02 | 隔离：线路故障 + 恢复并网 | `unknown` | 中文真实实体、多事件、日期级时间 |
| R03 | 隔离：需求冲击 | `unknown` | 亿千瓦、水平与增量并存、事后日期 |
| R04 | 隔离：新能源供给变化 | `unknown` | 出力纪录不是“预计增加”模板，数值是水平量 |
| R05 | 长期退役事件 | `unknown` | 英文、复数资产、“this decade”时间范围 |
| R06 | 双机跳闸事件 | `unknown` | 英文事件表时刻、来源市场时区、870 MW 损失 |
| R07 | 隔离：四类因素并存 | `unknown` | 多事件拆分、低风与燃气可用性方向语义 |
| R08 | 隔离：需求纪录 | `unknown` | 英文日期、需求水平、结算后修订语义 |
| R09 | 隔离：恢复并网但时间模糊 | `unknown` | “顺利并网”与“本轮高温前”不命中模板 |
| R10 | 无关社区服务 | `unknown/irrelevant` | 正确负对照 |

## 5. 主要问题

1. **高：分类词表只覆盖测试句式。** 中文真实表述“跳闸”“重新并网”“负荷创纪录”以及全部英文记录均被静默归为无关。
2. **高：实体规则只识别 `TEST_*` 和大写代码。** 中文电厂、机组、区域与自然语言资产名无法抽取。
3. **高：时间契约和解析器过窄。** 真实新闻大量只有日期、当地时刻、时间范围或“高温前”等模糊表达；当前仅接受 ISO 时刻和少量中文相对日时刻。
4. **高：量值固定为 MW 且语义混杂。** 万/亿千瓦需要换算；需求水平、出力水平、损失出力和变化量不能共用一个 `capacity_mw`。
5. **高：多事件只在命中多个模板后才隔离。** 当前连事件类别都没有命中，因此复杂新闻没有进入复核区，而是静默丢失。
6. **中：方向被事件类型硬编码。** 例如低风意味着可再生供给下降并可能形成上行压力，现有 `renewable_supply_change` 规则只能表达“预计增加/向下”。

没有产生误报不等于安全：系统把 9 篇含运行事件的新闻当成无关信息，真实运行会形成严重漏报。

## 6. 下一步门禁

当前规则抽取器继续作为显式模板回归基线，不再通过堆关键词扩展。结构化模型抽取代码边界已经完成，
详见[结构化模型新闻抽取实现说明](structured-news-extraction.md)。当前进度为：

1. **已完成**：模型原生结构化输出、字段级证据和失败隔离；
2. **已完成**：时间精度及原文绝对时间/组合时间/相对时间契约；
3. **已完成**：量值、单位、水平/变化/损失/恢复语义及确定性单位换算；
4. **已完成**：一篇文档多个事件候选、同文证据隔离和单次抽取缓存；
5. **待完成**：使用本语料评估真实模型，并建立不参与 Prompt 调整的盲测集；
6. **待完成**：真实模型达到门槛后再进入更大规模人工复核，不能用脚本化测试响应宣称生产可用。

## 7. 复现

```powershell
.\.venv\Scripts\python.exe scripts\benchmark_real_news_extraction.py

# 作为资格门禁使用；当前预期返回非零退出码
.\.venv\Scripts\python.exe scripts\benchmark_real_news_extraction.py --require-qualified

# 显式使用已配置的结构化模型；会访问相应模型端点
.\.venv\Scripts\python.exe scripts\benchmark_real_news_extraction.py `
  --extractor structured-model `
  --require-qualified

.\.venv\Scripts\python.exe -m pytest -q `
  tests/research/test_real_news_extraction_benchmark.py
```

本次规则基线结果产物：[real-news-extractor-be5a470945a6.json](../artifacts/acceptance/real-news-extractor-be5a470945a6.json)。基准脚本会在
`artifacts/acceptance/real-news-extractor-*.json` 保存逐案结果、指标、阈值、失败检查和源内容哈希。
