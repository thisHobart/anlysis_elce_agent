# P3 集成验收记录（2026-09-12）

## 结论

山东只读数据库取数、3折历史回测、次日96点预测、研究包生成、检查点和 Windows 打包链路均已跑通。真实运行不能判定预测增益：严格按锚点 `create_time` 截断后，前一日同刻基线在三折中仅有3个共同有效点，低于当前每折72点门禁。当前版本会继续输出未来预测，同时醒目标记“未验证出预测增益”。

## 真实数据运行

- 数据库：`original_data_liaocheng`，连接事务为 `READ ONLY`
- 实际价格范围：2025-11-01 00:15 至 2026-09-12 00:00
- 回测锚点：2026-08-28、2026-09-04、2026-09-11
- 预测范围：2026-09-13 00:00 至 23:45
- 实际价格新鲜度：超过2小时，未超过24小时拒绝线；已产生警告
- 预测 CSV：96行，96个有限值，范围约 206.47–458.96
- 回测逐点文件：288行
- 研究包清单：SHA-256 全部复核通过
- 业务数据库写入：0

三折共同有效点只有3个，因此下面数值仅用于验证计算链路，不能用于模型准入：模型 MAE 48.79、持续/前一日同刻 49.61、一周前同刻 93.92。当前代码要求至少216个共同有效点（三折各72点）后才可能判定预测增益。

## 自动测试

```text
ruff: passed
pytest: 692 passed in 104.34s
```

覆盖范围包括现有 EDA 回归、地区数据面板、旧会话迁移、历史版本可见性、防未来价格/实际负荷泄漏、确定性训练、4份输入哈希、取消后的折间恢复、研究包和桌面确认/结果卡。

## Windows 打包

两个模式均在构建后实际启动 EXE 并通过隔离运行探针：

| 模式 | 主程序大小 | 结果 |
|---|---:|---|
| onedir | 71,858,947 bytes | 通过 |
| onefile | 321,489,929 bytes | 通过 |

探针确认桌面窗口、2个内置 Skill、30个现有研究函数以及 PyTorch/scikit-learn P3 CPU 模型均可加载。PyInstaller 报告缺少可选 `tensorboard`，本次点预测不使用该组件，不影响探针。

## 未解决的数据限制

历史前一日价格版本的 `create_time` 晚于对应锚点，导致严格点时快照内的前一日同刻基线覆盖不足。不能通过读取锚点之后形成的版本补齐，否则会造成回测泄漏。后续若需要模型准入，应先确认源表的历史版本语义或提供锚点时真实可见的价格版本。
